import { Link, useParams } from 'react-router-dom';
import { findProductBySlug, products } from '../data/products';
import { ProductCard } from '../components/product/ProductCard';
import { Button } from '../components/ui/Button';
export function ProductDetail() { const { slug } = useParams(); const product = findProductBySlug(slug); if (!product) return <section className="page-hero"><h1>Piece not found</h1><p>This product may have moved.</p><Link to="/products">Back to products</Link></section>; const related=products.filter(p=>p.category===product.category&&p.id!==product.id).slice(0,3); return <><section className="detail"><div className="detail__visual"><span>{product.category}</span></div><div><p className="eyebrow">{product.category}</p><h1>{product.name}</h1><p className="lead">{product.description}</p><h2>{product.price}</h2><div className="meta"><p><strong>Dimensions</strong>{product.dimensions}</p><p><strong>Colors</strong>{product.colors.join(', ')}</p><p><strong>Materials</strong>{product.materials.join(', ')}</p></div><div className="actions"><Button to="/custom">Request this piece</Button><Button to="/custom" variant="secondary">Customise it</Button></div></div></section>{related.length>0&&<section className="section"><p className="eyebrow">Related</p><h2>Similar pieces</h2><div className="product-grid">{related.map(p=><ProductCard key={p.id} product={p}/>)}</div></section>}</>; }
